#include <stdio.h>
#include <ctype.h>

int main(int argc,char **argv) {
		int i;
		char *ch;
		
		for(i=1; i < argc; i++)
		{
		  for(ch=argv[i]; *ch != '\0'; ch++)
			{
				if( ! isdigit(*ch) )
					if( *ch == '.' )
						binary_print
					printf("ch: %c\n",*ch); 
				else
					printf("Error char: %c\n",*ch);
		return 0;
}
