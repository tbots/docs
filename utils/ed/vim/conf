
 Sourced files: 

	/usr/share/vim/vimrc		-
													 \ read please and note vimscript commands
	~/.vimrc


 Case of running vim as root but need a vimrc of the user that evaluated to root. Example from /usr/share/vim/vimrc
	
	if $SUDO_USER == 'osergiyuk'
 	 source /home/osergiyuk/.vimrc
	endif
