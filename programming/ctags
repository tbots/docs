
 --list-maps[=language|all]
 		output list of language mappings

 -R, --recurse[=yes|no]
 		recurse to the directories supplied on the command line 

 Ctrl+]
 		jump to the tag underneath the cursor

 :ts 
 		List the tags that match [indent], using the information in the tags file. When indent is not given, the last tag name from the
		tag stack is used. [indent] can be a regexp pattern, see |tag-regexp|. See |tag-priority| for the priorities used in the listing.
