#!/bin/bash
# Explore what is_code_in_var() function does

is_code_in_var() {
	grep -qP
	# -q, --quite, --silent			suppress all normal output
CODE_LIST_A="45911, 65912, 55721, 85331, 79041, 53242, 75051"
