
 'wrapper' branch notes.

  1. Continue on print_word(), what it does?
	1. Why struct scheme fmt is not global?
	2. Change in re_fdb() while condition 0 is simple false
	4. Compile via make(1)
	5. Change switch statement when checking for the proper escape sequence '\w..' to the case '[wW..
	6. Implement sets
	7. Allow more easy color selection and test
