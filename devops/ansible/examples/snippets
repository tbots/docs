ansible test -m shell -a 'cat /etc/passwd' | awk -F: '/\/home/ {print $1}'
ansible -m setup <host>
ansible <host> -m debug -a "msg={{ group_names }}"


# interesting.. sudo is not listed under module attributes; probably allowed on the task level
command: mknod -m 666 /dev/fuse e 10 229
sudo: yes
args:
  creates: /dev/fuse

# 
