
 [SYNTAX]

 Accepted file name representation when adding file type plugin: ext.vim, ext_name.vim, ext/name.vim.
 Default syntax files location is "$VIMRUNTIME/syntax".  To list all the syntax items use ":sy[ntax] [list]" commmand.
 To clear out old syntax definitions ":syntax clear".


 [FILES]

 $VIMRUNTIME/syntax.vim
 $VIMRUNTIME/filetype.vim
 $VIMRUNTIME/syntax/synload.vim

See defined colors in "$VIMRUNTIME/colors" directory. Test colors by running a ":runtime syntax/colortest.vim" command
Run :helptags to add documentation.
